# GitHub-hosted runner labels (ubuntu-latest, ubuntu-24.04-arm) are built into
# actionlint. Custom self-hosted labels were only needed for Blacksmith
# (blacksmith-*-ubuntu-2404{,-arm}); those were removed in the GitHub-hosted
# cutover trial. Re-add labels here if a self-hosted runner is introduced.
self-hosted-runner:
  labels: []
